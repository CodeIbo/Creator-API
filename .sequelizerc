require('dotenv').config();
const path = require("path");

console.log(__dirname)

const isProduction = process.env.NODE_ENV === 'production';
const resolvedPath = isProduction
  ? path.resolve(__dirname, './index.js')
  : path.resolve(__dirname, './src/config/sequelizer-path.config.ts');
const paths = require(resolvedPath).paths;

module.exports = {
  'config': paths.config,
  'models-path': paths.modelsPath,
  'seeders-path': paths.seedersPath,
  'migrations-path': paths.migrationsPath
};
